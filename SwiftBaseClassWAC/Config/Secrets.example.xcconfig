// Secrets.example.xcconfig
// Copy to Secrets.xcconfig (gitignored) and fill in. Shared.xcconfig includes
// Secrets.xcconfig optionally (#include?), so the build works without it.
//
//   cp SwiftBaseClassWAC/Config/Secrets.example.xcconfig \
//      SwiftBaseClassWAC/Config/Secrets.xcconfig
//
// Example third-party keys (referenced from the partial Info.plist if needed):
// ANALYTICS_API_KEY =
// MAPS_API_KEY =
